{
  "name": "inappbrowser-mira",
  "version": "0.1.0",
  "description": "Isolated OutSystems mobile WebView with temporary-browser authentication handoff",
  "license": "MIT",
  "cordova": { "id": "inappbrowser-mira", "platforms": ["android", "ios"] },
  "keywords": ["ecosystem:cordova", "outsystems", "authentication", "webview"],
  "scripts": { "test": "node --test tests/*.test.js", "check": "node --check www/MiraInAppBrowser.js && node --check web/mira-handoff.js" },
  "engines": { "node": ">=18" }
}
